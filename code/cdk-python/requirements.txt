# AWS CDK Python Requirements
# Core CDK library - provides fundamental CDK constructs and framework
aws-cdk-lib==2.167.1

# Constructs library - base library for all CDK constructs
constructs>=10.0.0,<11.0.0

# Development and testing dependencies (optional)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0

# Type checking for AWS CDK (optional but recommended)
# types-boto3>=1.0.0

# Note: This requirements.txt includes the minimal dependencies needed
# to deploy the timezone converter infrastructure. The CDK Python runtime
# includes all necessary libraries for timezone conversion (datetime, zoneinfo)
# without requiring additional packages.
#
# For development environments, uncomment the testing and linting tools above.
# For production deployments, consider pinning all dependency versions.